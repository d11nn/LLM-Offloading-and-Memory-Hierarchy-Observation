# Assignment 3 - LLM Offloading and Memory Hierarchy Observation

Student ID: 314581047

## Hardware / Environment

| Item | Value |
|---|---|
| Path | Google Colab |
| GPU | NVIDIA Tesla T4 |
| VRAM | 15360 MiB by `nvidia-smi`; 14912 MB by PyTorch |
| CPU | Colab x86_64 VM, 2 CPU reported by `iostat`; exact model was not printed in the notebook output |
| RAM | 12975 MB |
| Disk | Colab overlay filesystem, 113 GB total, device `sda` for Q4 extraction |
| Software | Python 3.12.13, PyTorch 2.10.0+cu128, Linux 6.6.122+ |

本次實驗使用 Colab Path。要注意的是，Colab 的 disk 是 overlay / shared cloud storage，所以 disk-offload 的 throughput 不應直接拿去和本機 NVMe 或 SATA SSD 比較；後面的分析只針對本次環境的相對差異。

## Q1. Progressive Offload Sweep

### Q1-1 Throughput and Peak GPU Memory

| Datapoint | Weight Distribution | Total Throughput (token/s) | Peak GPU Mem (GB) |
|---|---:|---:|---:|
| (1) | 100% GPU | 81.092 | 2.807 |
| (2) | 50% GPU + 50% CPU | 28.772 | 1.682 |
| (3) | 50% GPU + 50% Disk | 0.721 | 1.713 |
| (4) | 50% CPU + 50% Disk | 0.711 | 0.588 |
| (5) | 100% Disk | 0.357 | 0.620 |

從表格可以看到，GPU memory 隨著 weight 從 GPU 移出而下降；但是 throughput 不是線性下降，而是在 disk 介入時出現明顯斷崖。這代表本題真正限制吞吐量的不是模型計算量改變，而是 weight 每一步要從哪一層 memory hierarchy 被搬到 GPU。

### Q1-2 Boundary Ratios and Bottlenecks

| Boundary | Ratio | Dominant Bottleneck |
|---|---:|---|
| (1) to (2) | 2.82x | PCIe / host-to-device transfer |
| (2) to (3) | 39.91x | Disk |
| (3) to (4) | 1.01x | PCIe effect is almost hidden by Disk |
| (4) to (5) | 1.99x | Disk |

Front pair 是 (1) to (2), (2) to (3)；back pair 是 (3) to (4), (4) to (5)。

在 within-class comparison 裡，兩個 PCIe-related boundary 並不接近。(1) to (2) 是 2.82x，因為把一半 weight 從 GPU 改放 CPU 後，每一步需要走 CPU RAM 到 GPU 的 PCIe path；但 (3) to (4) 只有 1.01x，因為這兩個設定都已經有 50% weight 在 disk，disk latency 和 bandwidth 已經支配整體時間，CPU/GPU 之間的差異被 disk offload 蓋掉。

兩個 disk-related boundary 也不接近。(2) to (3) 是 39.91x，因為它把原本在 CPU RAM 的 50% weight 換成 disk，這是從 DRAM/PCIe 等級掉到 Colab overlay disk 等級；(4) to (5) 是 1.99x，因為原本就有 50% weight 在 disk，變成 100% disk 大致就是讓 disk-resident weight 量增加一倍，所以 back-pair ratio 小很多。

在 cross-class within-pair comparison 裡，front pair 的 Disk ratio 39.91x 明顯大於 PCIe ratio 2.82x。這符合 bandwidth order：PCIe/DRAM path 通常是 GB/s 到十幾 GB/s 等級，而本次 Colab `sda` 的 observed peak read bandwidth 是 255976 KB/s，約 250 MB/s。back pair 則是 Disk ratio 1.99x 大於 PCIe ratio 1.01x；原因是當 disk 已經在 critical path 上時，是否多走 CPU RAM 對總時間的影響很小，但 disk-resident weight 從 50% 增加到 100% 仍然會接近翻倍。

## Q2. Batch Size and I/O Amortization

### Q2-1 Batch Sweep under 100% Disk Offload

| GPU Batch Size | Wall Time / Total Latency (s) | Total Throughput (token/s) | Peak GPU Mem (GB) |
|---:|---:|---:|---:|
| 1 | 179.478 | 0.089 | 0.489 |
| 4 | 179.418 | 0.357 | 0.620 |
| 16 | 179.345 | 1.427 | 1.051 |

### Q2-2 Throughput Ratios

| Ratio | Measured |
|---|---:|
| b4 / b1 | 4.01x |
| b16 / b4 | 4.00x |

理論上 batch 每次乘以 4，如果每一批的 wall time 幾乎不變，throughput 就會接近 4 倍。本次結果剛好呈現這個行為：batch 1、4、16 的 total latency 都約 179 秒，代表 100% disk offload 時主要成本是把同一批模型 weight 從 disk 搬進來，而這個搬運成本可以被更多 prompts 分攤。因此 output tokens 變多，但是 wall time 幾乎不變，throughput 就接近 batch size 線性增加。

### Q2-3 Memory Scaling Limit

Batch 不能無限放大。從 b1 到 b16，peak GPU memory 從 0.489 GB 增加到 1.051 GB，原因是 KV cache 和 activation 在本作業固定留在 GPU。即使 weight 全部 offload 到 disk，batch 變大仍然會增加 cache / activation / temporary tensor 的 GPU footprint。當 batch 再繼續增加，最後會先碰到 GPU VRAM 上限，接著可能也會讓 CPU RAM 或 disk I/O queue 變成新的瓶頸。

## Q3. Weight Compression and Tier Interaction

### Q3-1 Compression Comparison

| Scenario | Compression | Total Throughput (token/s) | Peak GPU Mem (GB) |
|---|---|---:|---:|
| alpha: 100% GPU | No | 81.092 | 2.807 |
| alpha: 100% GPU | Yes | 23.218 | 1.104 |
| beta: 100% Disk | No | 0.357 | 0.620 |
| beta: 100% Disk | Yes | 1.732 | 0.470 |

### Q3-2 Interpretation

在 alpha，也就是 100% GPU baseline，`--compress-weight` 讓 throughput 從 81.092 降到 23.218 token/s，約是原本的 0.286x，等於 slowdown 3.49x。這裡 compression 確實降低了 weight footprint，所以 peak GPU memory 從 2.807 GB 降到 1.104 GB；但因為 weight 原本已經在 GPU 上，主要 bottleneck 不是 disk 或 PCIe transfer，而是每次使用 weight 前需要 de-quantization / unpack 的額外成本。對 100% GPU 來說，新增的解壓成本大於節省的 memory footprint，所以 throughput 下降。

在 beta，也就是 100% Disk offload，compression 讓 throughput 從 0.357 增加到 1.732 token/s，約 speedup 4.85x，peak GPU memory 也從 0.620 GB 降到 0.470 GB。這裡主要 bottleneck 是 disk I/O；compression 減少從 disk 讀出的 weight bytes，因此減少最慢那一層的搬運成本。雖然 de-quantization 仍然是新增成本，但在 disk-offload scenario 裡，減少 disk traffic 的效益更大，所以 throughput 上升。

這個變化不會簡單等於 4-bit compression 的 4x。第一，總時間不只有 weight read，還包含 prefill、decode compute、kernel launch、cache handling、de-quantization 和 Python/framework overhead。第二，Colab disk/overlay 與 page cache 會讓實際 I/O 行為不是理想線性模型。第三，compression 後還要把資料轉回 GPU 可計算的形式，所以省下的 bytes 不會全部轉成等比例吞吐量。也就是說，compression 改變的是 critical path 裡的一部分成本；當這部分是 bottleneck 時會加速，當它不是 bottleneck 時反而可能降速。

## Q4. I/O Behavior and Bottleneck Analysis under Disk Offload

### Q4-1 Disk I/O Pattern

| iostat (disk offload, `sda`) | Value |
|---|---:|
| r/s peak | 2583 req/s |
| rkB/s peak | 255976 KB/s |
| rareq-sz peak | 128.00 KB/req |
| %util peak | 80.8% |

本次 disk-offload 行為分類為 **A. Sequential, large blocks**。主要依據是 `rareq-sz peak = 128 KB/req`，明顯大於題目給的 32 KB 門檻，表示 kernel readahead 或 block layer 有把連續讀取合併成較大的 requests。同時 `rkB/s peak` 約 256 MB/s，`%util peak` 達 80.8%，代表 disk path 確實承受了明顯讀取壓力。這不像 C 類 page cache hit，因為 page cache hit 會看到很低的 disk read bandwidth；也不像 B 類 random small blocks，因為 request size 不是接近 4 KB 的小讀取。

補充觀察：`loop0` 也出現 `rareq-sz peak = 128 KB/req`、`%util peak = 97.9%`，這符合 Colab overlay / loop device 的特性。不過 requirement 的 awk 指定 disk name 使用 `sda`，所以上表以 `sda` 作為正式填答。

### Q4-2 Stage Timing

`q4_2_baseline.log` 有正確的 `--debug-mode breakdown` 輸出；但 `q4_2_disk.log` 的 notebook cell 註解寫著要加入 `--debug-mode breakdown`，實際命令卻漏掉該 flag，因此 disk-offload case 沒有原始 stage timing。下表對 baseline 使用原始 log 數值；對 disk-offload 的 `load_weight` 標成估算值，估算方式是用 normal run decode latency 差值除以 decoder layer-batches：`(168.145 - 0.685) / 750 = 0.223280 s`。這個值只能用來說明量級，不應視為 FlexGen breakdown 的直接量測。

| Scenario | load_weight (per-layer, sec) | compute_layer_decoding (per-batch, sec) | load / compute ratio |
|---|---:|---:|---:|
| (1) Baseline | 0.000036 | 0.006474 | 0.0056 |
| (5) Disk offload | 0.223280 (estimated) | 0.006474 (baseline compute reference) | 34.49 |

先看 `compute_layer_decoding`。Offload 改變的是 weight 放在哪一層 memory tier，以及每一步要從哪裡搬到 GPU；它不改變 OPT-1.3B 的 layer computation 本身，也不改變每個 token 需要跑過的模型結構。因此 GPU compute volume 理論上應該接近，真正放大的不是 compute，而是 load path。因為 disk case 缺少 debug breakdown 原始值，我在表中用 baseline compute 作為 reference；這個處理的重點是避免把總延遲差異錯誤歸因到 GPU compute。

再看 `load_weight`。Baseline 的 per-layer load_weight 是 0.000036 s，因為 weight 已在 GPU 上，load 幾乎只是 framework / scheduling 層級的成本。Disk-offload 的估算值是 0.223280 s，約比 baseline 大 `0.223280 / 0.000036 = 6202` 倍，也就是約 3.79 個 orders of magnitude。這代表 offload 改變的是資料供給路徑：從 GPU-local memory 變成 Colab disk / overlay 再進 GPU。Q1 的 throughput 斷崖、Q4 的 high disk utilization，以及這裡的 load_weight 量級差，都指向同一件事：100% disk offload 的瓶頸是 I/O supply，而不是模型算術量。

### Q4 Experiment Note

`q4_2_disk.log` 沒有 stage breakdown 是本次實驗資料的限制。若要讓 Q4 Part 2 完全符合原始要求，應重新執行：

```shell
python -m flexllmgen.flex_opt --model facebook/opt-1.3b \
    --percent 0 0 100 0 100 0 --gpu-batch-size 4 \
    --prompt-len 128 --gen-len 16 --offload-dir flexgen_offload \
    --debug-mode breakdown 2>&1 | tee logs/q4_2_disk.log
```

在不重新取得 Colab GPU runtime 的前提下，本報告選擇保守處理：所有直接量測值都來自現有 logs / notebook；缺失的 disk stage timing 只做明確標示的量級估算。

## Files for Submission

本報告對應的 mandatory monitoring logs 如下：

| Required File | Existing Path |
|---|---|
| `q4_1_io_iostat.log` | `logs/logs/q4_1_io_iostat.log` |
| `q4_1_io_free.log` | `logs/logs/q4_1_io_free.log` |
| `q4_2_baseline.log` | `logs/logs/q4_2_baseline.log` |
| `q4_2_disk.log` | `logs/logs/q4_2_disk.log` |

## Summary

整體結果可以用 memory hierarchy 的 bandwidth gap 解釋。只要 weight 留在 GPU，吞吐量最高；移到 CPU RAM 後會受到 PCIe transfer 影響，但仍維持在十幾到數十 token/s；一旦 disk 進入 critical path，throughput 下降到 1 token/s 以下。Batch size 在 100% disk offload 下幾乎線性改善 throughput，因為相同 weight I/O 被更多 prompts 分攤；但 batch 不能無限放大，因為 KV cache 和 activation 仍然消耗 GPU memory。Weight compression 則取決於 bottleneck：在 GPU baseline 中，de-quantization cost dominate，所以變慢；在 disk offload 中，I/O reduction dominate，所以變快。
